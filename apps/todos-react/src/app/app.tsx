import { ReactTodos } from '@nx-state-machine/react-todos';
import React from 'react';

import './app.css';

export const App = () => {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./app.css file.
   */
  return <ReactTodos />;
};

export default App;
