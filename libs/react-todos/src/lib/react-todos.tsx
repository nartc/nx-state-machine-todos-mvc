import React from 'react';

import './react-todos.css';

/* eslint-disable-next-line */
export interface ReactTodosProps {}

export const ReactTodos = (props: ReactTodosProps) => {
  return (
    <div>
      <h1>Welcome to react-todos component!</h1>
    </div>
  );
};

export default ReactTodos;
